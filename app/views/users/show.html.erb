<div class="container px-5 px-sm-0 mt-3">
<div class="row">

<div class="col-md-3">
<%= render 'books/list', book: @newbook , user: @user %>
</div>

<div class="col-md-8 offset-md-1">
<h2>Books</h2>

<table class="table table-hover table-inverse">
  <thead>
    <tr>
    <th></th>
    <th>Title</th>
    <th>Opinion</hr>
    </tr>
  </thead>
<% @book.each do |book| %>
  <tbody>
    <tr>
      <td><%= link_to user_path(@user) do %><%= image_tag @user.get_profile_image(80,80) %><% end %></td>
      <td><%= link_to book.title , book_path(book.id) %></td>
      <td><%= book.body %></td>
    </tr>
  </tbody>
<% end %>
</table>


</div>

</div>
</div>
